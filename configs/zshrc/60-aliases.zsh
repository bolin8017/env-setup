# ================================================================
# Load Custom Aliases
# ================================================================

[[ -f "${HOME}/.config/zsh/aliases.zsh" ]] && source "${HOME}/.config/zsh/aliases.zsh"
